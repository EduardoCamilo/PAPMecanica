@model IEnumerable<BaseModels.CondicaoPagamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeCondicaoPagamento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoCondicaoPagamento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoCondicaoPagamento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescontoCondicaoPagamento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AcrescimoCondicaoPagamento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeCondicaoPagamento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoCondicaoPagamento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoCondicaoPagamento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescontoCondicaoPagamento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AcrescimoCondicaoPagamento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CondicaoPagamentoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CondicaoPagamentoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CondicaoPagamentoID })
        </td>
    </tr>
}

</table>
