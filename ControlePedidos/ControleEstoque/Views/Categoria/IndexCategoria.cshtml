@model IEnumerable<BaseModels.Categoria>

@{
    ViewBag.Title = "IndexCategoria";
}

<h2>IndexCategoria</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescricaoCategoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescricaoCategoria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoriaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoriaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoriaID })
        </td>
    </tr>
}

</table>
